{
  "$schema": "https://playground.wordpress.net/blueprint-schema.json",
  "steps": [
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php';\nforeach ( array( 'post', 'page', 'attachment', 'revision', 'nav_menu_item' ) as $post_type ) {\n$posts = get_posts( array('posts_per_page' => -1, 'post_type' => $post_type ) );\nforeach ($posts as $post) wp_delete_post($post->ID, true);\n}"
    },
    {
      "step": "writeFile",
      "path": "/wordpress/wp-content/site-logo.png",
      "data": {
        "resource": "url",
        "url": "https://github.com/fellyph/blueprints-demos/blob/main/demos/assets/site-logo.png"
      }
    },
    {
      "step": "writeFile",
      "path": "/wordpress/wp-content/mu-plugins/rewrite.php",
      "data": "<?php add_action( 'after_setup_theme', 'set_existing_site_logo' ); function set_existing_site_logo() { if ( has_custom_logo() ) { return; } $logo_url = site_url( '/wp-content/site-logo.png' ); $logo_id = attachment_url_to_postid( $logo_url ); if ( $logo_id ) { set_theme_mod( 'custom_logo', $logo_id ); } } ?>"
    },
    {
      "step": "importWxr",
      "file": {
        "resource": "url",
        "url": "https://github.com/fellyph/blueprints-demos/blob/main/demos/content/blog-data.wxr"
      }
    },
    {
      "step": "setSiteOptions",
      "options": {
        "blogname": "Welcome to WordPress Playground",
        "blogdescription": "WordPress without a server",
        "show_on_front": "page",
        "page_on_front": 2
      }
    }
  ]
}
